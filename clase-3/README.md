# Resolución de Ejercicios JavaScript
**Alumno:** FRETE, Thiago Adrián  
**Email:** fretethiago08@gmail.com  

---

# Nota por Profesor Axel:

¡Excelente trabajo, Thiago! Resolviste la totalidad de los calentamientos, ejercicios de integración y el desafío integrador de `peaje.js` con muy buena lógica.

**Puntos destacados:**
- Correcto uso de `switch` y métodos de string como `.toUpperCase()`.
- Buena simulación aleatoria de la fila en `simularFilaCabina`.

**Recomendaciones docentes inyectadas en tus archivos:**
1. **`return` vs `console.log`:** En los ejercicios 9, 10, 12 y 13 hacías `console.log` dentro de la función. Salvo que la consigna lo pida explícitamente, las funciones deben RETORNAR el resultado (`return ...`) para que sea reutilizable.
2. **Declaración de variables:** Acordate de declarar siempre las variables con `let` o `const` (en `peaje.js` faltaba `let precio = 0`, lo que generaba una variable global accidental).
3. **Organización de funciones:** Evitamos declarar funciones dentro de un bucle `for` (como `numeroAleatorio`), es mejor tener la función afuera para no recrearla en cada vuelta del loop.

Muy buen trabajo, fijate en todas las notas que te dejé en los ejercicios, y cualquier duda me consultas!